# This blueprint is intentionally hidden from the public build. It is strictly reserved for internal development, testing, and debugging purposes
/blueprint/
/models/pad_selection.json

# IDE and local user state
.vs/
.vscode/
.vscode/*.local.json
CMakeUserPresets.json

# CMake build products
/build/
/out/

# Local runtime outputs
calibration/
camera_sample_*.jpg
/data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.dpapi
*.dpapi.new
edge-config.local.json

# Downloaded model binaries are machine-local artifacts. Track a manifest,
# checksum, and license/provenance information instead of committing binaries.
/models/**/*.onnx
/models/**/*.pth
